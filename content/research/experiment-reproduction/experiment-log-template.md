+++
title = "实验记录应该写什么"
date = 2026-05-18T11:20:00+08:00
draft = false
description = "整理实验记录的关键字段：假设、环境、步骤、数据、异常、结论和下一步。"
tags = ["实验记录", "复现", "研究方法"]
categories = ["研究课题"]
series = ["研究课题方法"]
slug = "experiment-log-template"
aliases = ["/research/experiment-log-template/"]
toc = true
+++

## 实验记录的价值

实验记录不是流水账，而是为了让未来的自己能够复现当时的判断。好的实验记录应该能回答：为什么做这个实验，怎么做的，结果是什么，是否支持原假设。

## 推荐字段

可以固定记录这些字段：

```txt
实验日期：
实验目标：
假设：
环境：
输入数据：
操作步骤：
关键参数：
结果：
异常：
结论：
下一步：
```

字段不一定每次都写满，但假设、环境、结果和结论最好保留。

## 环境记录

环境包括硬件、系统版本、依赖版本、编译参数和数据版本。很多复现失败不是方法错了，而是环境差异没有记录。

嵌入式相关实验还要记录板卡型号、内核版本、设备树版本、启动参数和外设连接状态。

## 异常也要记录

失败实验同样有价值。异常日志、错误输入和失败条件能帮助排除路径，也能避免之后重复踩同一个问题。

## 小结

实验记录的目标是支持复盘和复现。只要能让结论有证据、有上下文、有下一步，它就是有效记录。
